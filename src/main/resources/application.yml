quartz:
  enabled: true
spring:
  thymeleaf:
    mode: LEGACYHTML5
  profiles:
     active: serve

  application:
    name: wisdomhydrologic
mybatis:
  configuration:
    mapUnderscoreToCamelCase: true
version:
  calversion: "V1.1" # 版本号
  build: 1111 # 小版本号

server:
  port: 8097
  context-path: /wisdomhydrologic
